// src/test/extension.test.ts
import * as assert from 'assert';

// This is a dummy test file to satisfy the compiler
suite('Extension Test Suite', () => {
	test('Dummy test', () => {
		assert.strictEqual(-1, [1, 2, 3].indexOf(5));
	});
});
